name: damath
description: math extension for dart.
version: 0.0.8
repository: https://github.com/nomagicisreal/damath

environment:
  sdk: ^3.2.5

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints: ^2.1.0
  test: ^1.24.0
