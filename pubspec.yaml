name: damath
description: math collection for dart.
version: 0.0.26
repository: https://github.com/nomagicisreal/damath

environment:
  sdk: ^3.2.5

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  lints: ^5.0.0
  test: ^1.24.0

flutter:
  uses-material-design: true